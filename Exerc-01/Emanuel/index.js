/**
 * 1. Dado o tamanho da base e da altura de um retângulo, calcular a sua área e o seu perímetro.
 * Area = Mult base * Altura / 2
*/

let areaTotal = 0
function area(base, altura) {
    return areaTotal = (base * altura) / 2
}

console.log(area(10, 10))